<!-- Shared Footer -->
<footer class="bg-gray-900 text-white py-10">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex flex-col md:flex-row items-center justify-between gap-6">
            <div class="flex items-center gap-6">
                <a href="{{ path('app_home') }}" class="block shrink-0" aria-label="LearnAdapt home">
                    <img src="/brand/learnadapt-premium-logo.svg" alt="LearnAdapt" class="block h-12 w-auto" style="height:56px;width:auto;max-width:min(240px,60vw);">
                </a>
                <nav class="flex gap-6 text-sm">
                    <a href="{{ path('app_home') }}" class="text-white/60 hover:text-white transition">Home</a>
                    <a href="{{ path('app_profile') }}" class="text-white/60 hover:text-white transition">Profile</a>
                    <a href="{{ path('app_settings') }}" class="text-white/60 hover:text-white transition">Settings</a>
                </nav>
            </div>
            <div class="flex items-center gap-3">
                <a href="#" class="w-8 h-8 bg-white/10 rounded-full flex items-center justify-center hover:bg-white/20 transition">
                    <span class="material-icons text-sm">public</span>
                </a>
                <a href="#" class="w-8 h-8 bg-white/10 rounded-full flex items-center justify-center hover:bg-white/20 transition">
                    <span class="material-icons text-sm">photo_camera</span>
                </a>
            </div>
        </div>
        <div class="border-t border-white/10 mt-6 pt-5 text-center">
            <p class="text-sm text-white/30">Copyright &copy; {{ "now"|date("Y") }} Learn Adapt. All rights reserved.</p>
        </div>
    </div>
</footer>
